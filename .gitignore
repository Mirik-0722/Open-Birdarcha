node_modules/
dist/
target/
.idea/
*.iml
.DS_Store

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Local DB dump (dev seed) — commit qilinmaydi
dev-db-dump.sql

# Deploy sirlari — .env commit qilinmaydi (.env.example qoladi)
.env

# Claude worktree'lari — alohida git repo, commit qilinmaydi
.claude/worktrees/
